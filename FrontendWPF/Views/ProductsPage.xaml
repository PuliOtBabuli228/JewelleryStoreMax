<Page x:Class="FrontendWPF.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:FrontendWPF.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="675" d:DesignWidth="850"
      Title="ProductsPage">

    <Page.Resources>
        <Style x:Key="ProductButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="210"/>
            <Setter Property="Width" Value="165"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFEFEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ProductImageStyle" TargetType="Image">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Stretch" Value="UniformToFill"/>
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками действий -->
        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Height="60">
            <Button Content="Добавить товар" Width="150" Height="40" Margin="5" Command="{Binding AddProductCommand}"/>
            <Button Content="Редактировать" Width="150" Height="40" Margin="5" Command="{Binding EditProductCommand}" IsEnabled="{Binding IsProductSelected}"/>
            <Button Content="Удалить" Width="150" Height="40" Margin="5" Command="{Binding DeleteProductCommand}" IsEnabled="{Binding IsProductSelected}"/>
            <TextBox Width="300" Height="30" Margin="20,0,0,0" VerticalContentAlignment="Center" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Панель товаров -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl ItemsSource="{Binding Products}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ProductButtonStyle}" Command="{Binding DataContext.SelectProductCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}">
                            <StackPanel Orientation="Vertical">
                                <Image Source="{Binding Image}" Style="{StaticResource ProductImageStyle}"/>
                                <Label Content="{Binding Name}" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <Label Content="{Binding Price}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
